node_modules
dist
.DS_Store
*.log
.vite
.direnv
result

# Unused source audio (boss tracks, theme drafts) — keep out of git, opt back
# in by moving the file out of src/assets/audio/tracks/ when you wire it up.
src/assets/audio/tracks/
