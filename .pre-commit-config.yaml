repos:
  - repo: local
    hooks:
      - id: biome-format
        name: biome format
        entry: biome format --write --no-errors-on-unmatched
        language: system
        types_or: [javascript, jsx, ts, tsx, json]
